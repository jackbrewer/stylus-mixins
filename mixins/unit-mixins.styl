//
// UNIT MIXINS
// ===========
// Mixins which return a unit value
//


//
// EM
// Returns an em, based on a pased pixel value. Has an optional base value.
//

em($px, $base = $baseFontSize)
  ($px / $base) em


//
// RATIO PERCENTAGE
// Returns an percentage ratio, based on two passed values
//

ratio-percentage(a, b)
  ( a / b  * 100) %
