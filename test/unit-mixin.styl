// @it should convert unit into em value with $baseFontSize
$baseFontSize = 16px

.foo
  width em(16)

// @expect
.foo {
  width: 1em;
}

// @it should convert unit into em value with font size parameter
.foo
  width em(16, 32)

// @expect
.foo {
  width: 0.5em;
}