// @describe ellipsis()

// @it default to output overflow hidden, text overflow ellipsis, and whitespace nowrap
.element
  ellipsis()

// @expect
.element {
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
}

// @it no-wrap false to output overflow hidden, and text overflow ellipsis
.element
  ellipsis(false)

// @expect
.element {
  text-overflow: ellipsis;
  overflow: hidden;
}
